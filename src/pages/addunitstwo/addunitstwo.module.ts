import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { AddunitstwoPage } from './addunitstwo';

@NgModule({
  declarations: [
    AddunitstwoPage,
  ],
  imports: [
    IonicPageModule.forChild(AddunitstwoPage),
  ],
  exports: [
    AddunitstwoPage
  ]
})
export class AddunitstwoPageModule {}
